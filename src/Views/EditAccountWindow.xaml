<!-- Copyright (c) CSUploader. All rights reserved. -->
<!-- Licensed under the MIT license. See LICENSE file in the project root for full license information. -->

<Window x:Class="CSUploader.Views.EditAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Account"
        Width="450" Height="240"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="File Hoster:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="HosterCombo" Margin="0,0,0,8" Padding="4,4" FontSize="12" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Username:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="UsernameBox" Margin="0,0,0,8" Padding="4,3" FontSize="12" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Password:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="PasswordBox" Margin="0,0,0,8" Padding="4,3" FontSize="12" />

        <CheckBox Grid.Row="3" Grid.Column="1" x:Name="EnabledCheck" Content="Account enabled" Margin="0,0,0,8" />

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Height="28" Margin="0,0,8,0" Click="SaveButton_Click"
                    Style="{StaticResource PrimaryButtonStyle}" />
            <Button Content="Cancel" Width="80" Height="28" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
